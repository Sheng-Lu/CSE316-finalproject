import React from 'react';

const Logo = (props) => {
    return (
        <div className='logo' style={{color: "orange"}}>
            The World Data Mapper
        </div>
    );
};

export default Logo;